<% Role.where("name like '%client%'").each do |role|%>
<% value = params[:membership].present? ? params[:membership][:role_ids].include?(role.id.to_s) : false %>
<%= check_box_tag "membership[role_ids][]", role.id, value %><%= role %>
<% end %>